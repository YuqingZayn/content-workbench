# 任务进度

V0.2 已完成用户确认的本地实现和模拟验收，真实平台验收延期。第一闭环历史如下。原始规格的完整任务含多项增强验收，不因核心路径可用就把全部细项勾选为完成。

| 原任务 | 当前实现 | 状态 |
|---|---|---|
| T001 | 独立 Electron / React / TS，类型 IPC，依赖锁与 Windows 打包 | 完成并验证 |
| T002 | CLI 发现、握手、账号、模型目录、生成协议 0.153.3 | 真实验证通过 |
| T003 | PNG / JPEG / WebP、H.264/AAC MP4、Range、关键帧 | 真实桌面验证通过；未内置 FFmpeg |
| T004 | 九平台辅助能力与未验证自动能力表 | 完成本地范围 |
| T005–T006 | 文件夹、身份切换、稳定 ID、schema、只读、重复 ID 检查 | 核心完成；未来 schema 提示后拒绝写入 |
| T007 | 日志恢复、历史、外部正文检测、版本冲突与合并 | 核心测试通过 |
| T008 | 项目锁、备份、移动、恢复后暂停 | 核心测试通过 |
| T009–T011 | 文件导入 / 拖放 / 粘贴、复制引用、去重、重连、预览、关键帧与裁剪 | 核心完成；0.1.1 已实现图片后台 Worker、缩略图缓存及详情分级预览；60 项分页，未实现视频转码 |
| T012–T013 | 底稿、独立平台语言版本、媒体顺序 / 封面、账号与目标 | 核心完成 |
| T014–T015 | 快照、逐目标包、部分回填、日历改期 / 撤销 / 暂停 / 取消 | 核心测试通过 |
| T016–T017 | 真实多轮、停止、队列、身份上下文、图片输入、结构化落盘与冲突建议 | 真实生成通过；冲突、未知素材、过期终态通过事件模拟测试 |
| T018 | 安装 / 便携包、使用文档、第一闭环桌面测试 | 核心闭环完成并验证；完整原规格增强项见下文 |
| T019–T020 | 发布状态机、Mock、租约、恢复、后台与托盘 | 本地验收通过 |
| T021–T024 | Discord / X / IG / Facebook Page 图文适配器 | 接口模拟通过；真实账号未验证 |
| T025 | v1 → v2 升级、断开、备份恢复、凭据隔离与能力标识 | 本地通过；外部回执延期，本次不打包 |
| T026–T028 | 扩展渠道、指标等 | 后续范围，未实施 |

## 后续细化

- 大规模导入的进度与取消、视频缩略图；图片缩略图工作池与磁盘缓存已在 0.1.1 实现，原素材导入仍串行处理、渲染分页。
- 日历的目标时区直接输入和夏令时歧义选择；现版以明确的本机时区输入，UTC 存储并显示项目时区偏移。
- 更完整的只读新 schema 浏览与复制为新 projectId；现版拒绝未知 schema，提供明确的恢复到新位置流程。
- 素材标签筛选、内容归档、跨项目复制、选题批量生成、任务快照更新等运营增强。
- V0.2 后续按实际批准的目标完成真实发送、权限和配额验收；当前实现与故障恢复证据见 [V0.2 进度](v0.2-progress.md)。

## 0.2.1 小红书网页登录

按用户新要求新增官方网页登录、项目 / 账号独立持久会话、身份检查、失效和网络异常状态、本机退出。真实匿名登录入口已验证；扫码由用户完成，不把网页登录当作小红书自动发布。见 [操作说明](xiaohongshu-login.md)。
